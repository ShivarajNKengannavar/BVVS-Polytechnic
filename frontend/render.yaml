services:
  - type: web
    name: frontend
    env: static
    staticPublishPath: dist/bvvs-frontend/browser
    buildCommand: npm install && npm run build
    startCommand: null
    routes:
      - type: rewrite
        source: /*
        destination: /index.html